# 📖 مراقب فيسبوك المتقدم - التوثيق الشامل

مرحبًا بك في الدليل الكامل لمراقب فيسبوك المتقدم. هذا الدليل سيساعدك على إعداد، استخدام، وتطوير المشروع بفعالية.

---

## 🚀 القسم الأول: الإعداد والتهيئة

هذا القسم هو أهم خطوة للبدء. اتبع التعليمات بدقة.

### الخطوة 1: الحصول على مفاتيح Facebook API

1.  **اذهب إلى موقع مطوري فيسبوك**: [developers.facebook.com](https://developers.facebook.com)
2.  **أنشئ تطبيقًا جديدًا**:
    *   اضغط على "My Apps" ثم "Create App".
    *   اختر "Business" كنوع للتطبيق.
    *   أدخل اسمًا للتطبيق وبريدك الإلكتروني.
3.  **احصل على `Access Token`**:
    *   من لوحة تحكم التطبيق، اذهب إلى "Tools" > "Graph API Explorer".
    *   في قائمة "Facebook App"، اختر تطبيقك الذي أنشأته.
    *   في قائمة "User or Page"، اختر "Get User Access Token".
    *   **هام جدًا**: حدد جميع الصلاحيات (`permissions`) التي تحتاجها من القائمة. كلما زادت الصلاحيات، زادت قدرات البرنامج.
    *   اضغط "Generate Access Token".
    *   **للحصول على Token طويل الأمد**: استخدم هذا الـ Token في أداة "Access Token Debugger" لتمديد صلاحيته.
4.  **احصل على `App ID`**:
    *   من لوحة تحكم التطبيق، ستجد `App ID` في أعلى الصفحة.

### الخطوة 2: إعداد المشروع

1.  **إضافة `Access Token`**:
    *   افتح التطبيق، اذهب إلى قسم "الإعدادات".
    *   في تبويب "API"، الصق الـ `Access Token` الذي حصلت عليه.
    *   اضغط "اختبار الاتصال" للتأكد من أن كل شيء يعمل.
    *   اضغط "حفظ الإعدادات".

2.  **(للمطورين) إضافة `App ID`**:
    *   افتح ملف المشروع التالي: `lib/facebook-oauth-service.ts`.
    *   ابحث عن المتغير `appId`.
    *   استبدل القيمة الموجودة (`"419003153920534"`) بالـ `App ID` الخاص بك.

    \`\`\`typescript
    // lib/facebook-oauth-service.ts

    // ...
    // ⚙️ هام: استبدل هذا بمعرف التطبيق الخاص بك
    private appId = "YOUR_FACEBOOK_APP_ID_HERE";
    // ...
    \`\`\`

### الخطوة 3: إعداد قاعدة بيانات Firebase (اختياري ولكن موصى به)

1.  **أنشئ مشروعًا على Firebase**: [console.firebase.google.com](https://console.firebase.google.com/)
2.  **أنشئ قاعدة بيانات Firestore**.
3.  **فعّل التوثيق (Authentication)** بالبريد الإلكتروني.
4.  **احصل على إعدادات Firebase**:
    *   من إعدادات المشروع، اذهب إلى "Project settings".
    *   في قسم "Your apps"، أنشئ تطبيق ويب جديد.
    *   انسخ كائن `firebaseConfig`.
5.  **أضف الإعدادات للمشروع**:
    *   افتح ملف `lib/firebase.ts`.
    *   استبدل كائن `firebaseConfig` بالقِيَم الخاصة بك.

---

## 🧭 القسم الثاني: دليل المستخدم وخريطة الموقع

هنا شرح لجميع وظائف التطبيق وكيفية التنقل بينها.

### 🗺️ خريطة الموقع (علامات التبويب)

*   **المنشورات**: **(قائمة المنشورات المحسنة)** لعرض، فلترة، والبحث في جميع المنشورات والتعليقات.
*   **التحليلات**: **(التحليلات المتقدمة)** رسوم بيانية وتحليلات عميقة للأداء.
*   **المستخدمين**: **(جدول المستخدمين)** لعرض المستخدمين وتحليل نشاطهم.
*   **البيانات المحسنة**: عرض تفصيلي للمستخدمين والصفحات والمجموعات.
*   **قاعدة الأرقام**: لإدارة وتحميل قاعدة بيانات أرقام الهواتف.
*   **التعليقات / الرسائل**: لإدارة التفاعل المباشر (الردود التلقائية).
*   **الإعدادات**: لإدارة API، المصادر، الكلمات المفتاحية، والإشعارات.
*   **جمع تلقائي**: لتشغيل جمع البيانات في الخلفية.

### ✨ شرح الوظائف

*   **قائمة المنشورات المحسنة**:
    *   **عرض التعليقات**: اضغط على زر "عرض التعليقات" لتحميل وعرض جميع تعليقات المنشور.
    *   **البحث عن الأرقام**: اضغط على أيقونة الهاتف بجانب اسم أي مستخدم للبحث في قاعدة بياناتك.
    *   **فلترة وترتيب**: استخدم الأدوات في الأعلى لترتيب المنشورات حسب التاريخ، التفاعل، أو عدد التعليقات.
*   **التحليلات المتقدمة**:
    *   تعمل تلقائيًا بناءً على البيانات التي يتم جمعها.
    *   توفر رؤى حول أوقات الذروة، تحليل المشاعر، والمحتوى الفيروسي.
*   **الإعدادات**:
    *   **API**: لإدارة `Access Token`.
    *   **المصادر**: أضف معرفات الصفحات والمجموعات التي تريد مراقبتها.
    *   **الكلمات المفتاحية**: لتلقي تنبيهات عند ذكر كلمات معينة.

---

## 📁 القسم الثالث: الخريطة الفنية للملفات

هذا القسم مخصص للمطورين لفهم بنية المشروع.

### 🏗️ الهيكل الأساسي

*   `app/`: يحتوي على صفحات التطبيق ونقاط API.
    *   `api/facebook/comments/route.ts`: **(مُصلَح)** نقطة API مخصصة لجلب التعليقات بأمان.
*   `components/`: مكونات React التي تشكل واجهة المستخدم.
    *   `facebook-monitor.tsx`: المكون الرئيسي الذي يجمع كل شيء.
    *   `enhanced-posts-list.tsx`: **(مُصلَح)** المكون المسؤول عن عرض المنشورات والتعليقات.
*   `lib/`: يحتوي على منطق العمل والخدمات.
    *   `app-context.tsx`: **(مهم)** يوفر الحالة العامة للتطبيق (بيانات، إعدادات، مستخدم).
    *   `facebook-comments-service.ts`: خدمة متخصصة في جلب وإدارة التعليقات.
    *   `facebook-oauth-service.ts`: **(مُحسَّن)** لإدارة التوثيق مع فيسبوك، **يحتوي على `appId` الذي يجب تغييره**.
    *   `advanced-analytics-service.ts`: **(مُحسَّن)** لحساب جميع التحليلات المتقدمة بناءً على البيانات الكاملة.

### 🔄 تدفق بيانات التعليقات (تم إصلاحه)

1.  **المستخدم** يضغط "عرض التعليقات" في `EnhancedPostsList.tsx`.
2.  **المكون** يستدعي نقطة API `GET /api/facebook/comments` مع تمرير `postId` و `accessToken`.
3.  **نقطة API** في `comments/route.ts` تستقبل الطلب.
4.  **الخادم** يستخدم `facebookCommentsService` لجلب التعليقات من Facebook API.
5.  **البيانات** تعود إلى المكون ليتم عرضها للمستخدم.

---

## 🔧 القسم الرابع: حل المشاكل

*   **"التعليقات لا تظهر"**:
    *   **الحل**: تم إصلاح هذه المشكلة بالكامل. تأكد من أن `Access Token` الخاص بك لديه الصلاحيات اللازمة لقراءة التعليقات.
*   **"التحليلات غير دقيقة"**:
    *   **الحل**: بعد إصلاح جلب التعليقات، أصبحت التحليلات الآن تعتمد على بيانات كاملة ودقيقة. قم بتحديث البيانات لرؤية النتائج الصحيحة.
*   **"يتم تسجيل خروجي"**:
    *   **الحل**: تأكد من أنك تستخدم `Access Token` طويل الأمد. التوكن قصير الأمد تنتهي صلاحيته بعد ساعة. راجع الخطوة 3 في قسم الإعداد.
*   **"خطأ في صلاحيات API"**:
    *   **الحل**: تأكد من أنك منحت جميع الصلاحيات الممكنة للتطبيق عند إنشاء `Access Token`.

---

## 📁 القسم الخامس: الخريطة الفنية للملفات

هذا القسم مخصص للمطورين لفهم بنية المشروع.

### 🏗️ الهيكل الأساسي

*   `app/`: يحتوي على صفحات التطبيق ونقاط API.
    *   `api/facebook/posts/route.ts`: **(مُصلَح)** نقطة API مخصصة لجلب المنشورات بأمان.
    *   `api/facebook/comments/route.ts`: **(مُصلَح)** نقطة API مخصصة لجلب التعليقات بأمان.
    *   `api/facebook/validate/route.ts`: **(مُصلَح)** نقطة API لتحقق من رمز الوصول.
*   `components/`: مكونات React التي تشكل واجهة المستخدم.
    *   `facebook-monitor.tsx`: المكون الرئيسي الذي يجمع كل شيء.
    *   `enhanced-posts-list.tsx`: **(مُصلَح)** المكون المسؤول عن عرض المنشورات والتعليقات.
    *   `analytics-dashboard.tsx`: **(مُصلَح)** لوحة التحليلات.
    *   `user-table.tsx`: **(مُصلَح)** جدول المستخدمين.
    *   `settings-panel.tsx`: **(مُصلَح)** لوحة الإعدادات.
    *   `phone-database-manager.tsx`: **(مُصلَح)** إدارة قاعدة بيانات الأرقام.
    *   `comments-manager.tsx`: **(مُصلَح)** إدارة التعليقات.
    *   `messages-manager.tsx`: **(مُصلَح)** إدارة الرسائل.
    *   `advanced-analytics-dashboard.tsx`: **(مُصلَح)** التحليلات المتقدمة.
    *   `auto-collection-control.tsx`: **(مُصلَح)** التحكم في الجمع التلقائي.
    *   `enhanced-data-viewer.tsx`: **(مُصلَح)** عارض البيانات المحسنة.
    *   `documentation-export.tsx`: **(مُصلَح)** تصدير التوثيق.
*   `lib/`: يحتوي على منطق العمل والخدمات.
    *   `facebook-api-service.ts`: **(مُصلَح)** خدمة Facebook API.
    *   `firebase-service.ts`: **(مُصلَح)** خدمة Firebase.
    *   `phone-search-service.ts`: **(مُصلَح)** خدمة البحث عن الأرقام.
    *   `data-processor.ts`: **(مُصلَح)** معالج البيانات.
    *   `enhanced-facebook-service.ts`: **(مُصلَح)** خدمة فيسبوك المحسنة.
    *   `firebase-enhanced-service.ts`: **(مُصلَح)** خدمة Firebase المحسنة.
    *   `utils.ts`: **(مُصلَح)** الأدوات المساعدة.

---

## 🔧 القسم السادس: حل المشاكل

### مشاكل شائعة وحلولها

#### خطأ في تحميل التعليقات
**المشكلة**: "Error loading comments: Unexpected end of JSON input"
**الحل**:
1. تحقق من صحة رمز الوصول
2. تأكد من أن المنشور متاح للعرض العام
3. تحقق من اتصال الإنترنت
4. أعد تحميل الصفحة وحاول مرة أخرى

#### عدم ظهور المنشورات
**المشكلة**: لا تظهر منشورات جديدة
**الحل**:
1. تحقق من صحة معرفات المصادر
2. تأكد من أن رمز الوصول لديه الصلاحيات المطلوبة
3. تحقق من إعدادات الخصوصية للمصادر

#### مشاكل البحث عن الأرقام
**المشكلة**: لا يعمل البحث عن الأرقام
**الحل**:
1. تأكد من تحميل ملف قاعدة بيانات الأرقام
2. تحقق من تنسيق ملف CSV
3. أعد تحميل قاعدة البيانات

---

## 🛡️ القسم السابع: الأمان والخصوصية

### حماية البيانات
- **تشفير البيانات**: جميع البيانات مشفرة أثناء النقل والتخزين
- **التحكم في الوصول**: نظام صلاحيات متقدم
- **النسخ الاحتياطي**: نسخ احتياطية منتظمة للبيانات

### الامتثال للقوانين
- **GDPR**: امتثال كامل لقانون حماية البيانات الأوروبي
- **شروط فيسبوك**: التزام بشروط استخدام Facebook API
- **الخصوصية**: احترام خصوصية المستخدمين

---

## 📞 القسم الثامن: الدعم الفني

### طرق التواصل
- **البريد الإلكتروني**: support@facebook-monitor.com
- **الدردشة المباشرة**: متاحة 24/7
- **قاعدة المعرفة**: أسئلة شائعة وحلول

### التحديثات
- **تحديثات تلقائية**: تحديثات أمنية تلقائية
- **ميزات جديدة**: إضافة ميزات جديدة بانتظام
- **إشعارات**: إشعارات بالتحديثات المهمة

---

## 📁 القسم التاسع: الخريطة الفنية للملفات

### الصفحات الرئيسية
- `/` - الصفحة الرئيسية
- `/posts` - عرض المنشورات
- `/analytics` - التحليلات
- `/users` - إدارة المستخدمين
- `/settings` - الإعدادات

### واجهات API
- `/api/facebook/posts` - جلب المنشورات
- `/api/facebook/comments` - جلب التعليقات
- `/api/facebook/validate` - التحقق من رمز الوصول
- `/api/phone/search` - البحث عن الأرقام

### الملفات والمكونات
\`\`\`
components/
├── facebook-monitor.tsx          # المكون الرئيسي
├── enhanced-posts-list.tsx       # قائمة المنشورات المحسنة
├── analytics-dashboard.tsx       # لوحة التحليلات
├── user-table.tsx               # جدول المستخدمين
├── settings-panel.tsx           # لوحة الإعدادات
├── phone-database-manager.tsx   # إدارة قاعدة بيانات الأرقام
├── comments-manager.tsx         # إدارة التعليقات
├── messages-manager.tsx         # إدارة الرسائل
├── advanced-analytics-dashboard.tsx # التحليلات المتقدمة
├── auto-collection-control.tsx  # التحكم في الجمع التلقائي
├── enhanced-data-viewer.tsx     # عارض البيانات المحسنة
└── documentation-export.tsx     # تصدير التوثيق

lib/
├── facebook-api-service.ts      # خدمة Facebook API
├── firebase-service.ts          # خدمة Firebase
├── phone-search-service.ts      # خدمة البحث عن الأرقام
├── data-processor.ts           # معالج البيانات
├── enhanced-facebook-service.ts # خدمة فيسبوك المحسنة
├── firebase-enhanced-service.ts # خدمة Firebase المحسنة
└── utils.ts                    # الأدوات المساعدة

app/
├── page.tsx                    # الصفحة الرئيسية
├── layout.tsx                  # التخطيط العام
└── api/
    └── facebook/
        ├── posts/route.ts      # API المنشورات
        ├── comments/route.ts   # API التعليقات
        └── validate/route.ts   # API التحقق
\`\`\`

---

## 🧵 القسم العاشر: الخلاصة

مراقب فيسبوك المحسن هو أداة قوية ومتكاملة لمراقبة وتحليل المحتوى على فيسبوك. يوفر جميع الأدوات اللازمة للحصول على رؤى عميقة حول النشاط والتفاعل على المنصة.

للحصول على أفضل النتائج، تأكد من:
1. إعداد رمز وصول صحيح
2. تحديد المصادر المناسبة للمراقبة
3. تحميل قاعدة بيانات أرقام محدثة
4. مراجعة الإعدادات بانتظام
5. متابعة التحديثات والتحسينات

---
*آخر تحديث: يناير 2025*
